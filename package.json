{
  "name": "scriben",
  "version": "0.1.0",
  "description": "Secure document editor with Filen sync integration.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy",
    "preview:worker": "npm run build && wrangler dev"
  },
  "dependencies": {
    "@filen/sdk": "^0.1.171"
  },
  "devDependencies": {
    "vite": "^8.0.14",
    "vite-plugin-node-polyfills": "^0.28.0",
    "wrangler": "^4.95.0"
  }
}
